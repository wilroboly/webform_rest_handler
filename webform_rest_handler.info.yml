name: Webform REST Handler
type: module
description: Permits the pushing of webform submissions to RESTful endpoints using XML, JSON or Form encoding.
core: 8.x
package: Webform
dependencies:
  - drupal:webform
